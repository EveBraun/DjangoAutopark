
from django.contrib import admin
from django.urls import path, include
from drivers import urls as autopark_urls


urlpatterns = [
    # path('admin/', admin.site.urls),
    # path('drivers/', include(autopark_urls))
]